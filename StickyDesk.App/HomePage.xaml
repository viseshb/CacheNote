<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="StickyDesk_App.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:StickyDesk_App.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:StickyDesk_App.Models"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="Transparent"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="CardButton" TargetType="Button">
            <Setter Property="Background" Value="{ThemeResource AppSurfaceBrush}" />
            <Setter Property="Padding" Value="20" />
            <Setter Property="MinHeight" Value="140" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Top" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{ThemeResource AppBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="14">
                            <!-- VSM must live on the template ROOT element, or GoToState never finds it. -->
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="Shade.Opacity" Value="1" />
                                            <Setter Target="Root.BorderBrush" Value="{ThemeResource AppAccentBrush}" />
                                            <Setter Target="Root.(Border.BorderThickness)" Value="2" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="ShadePressed.Opacity" Value="1" />
                                            <Setter Target="Root.BorderBrush" Value="{ThemeResource AppAccentBrush}" />
                                            <Setter Target="Root.(Border.BorderThickness)" Value="2" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled" />
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Grid>
                                <!-- hover + press shades (behind content) -->
                                <Border x:Name="Shade" Background="#382563EB" CornerRadius="14" Opacity="0" />
                                <Border x:Name="ShadePressed" Background="#552563EB" CornerRadius="14" Opacity="0" />
                                <ContentPresenter
                                    Margin="{TemplateBinding Padding}"
                                    HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}" />
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <ScrollViewer HorizontalScrollMode="Disabled" VerticalScrollBarVisibility="Auto">
        <Grid HorizontalAlignment="Center" MaxWidth="820" Padding="40,28,40,48">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="4,0,0,24" Spacing="4">
                <TextBlock
                    FontSize="32"
                    FontWeight="SemiBold"
                    Foreground="{ThemeResource AppTextPrimaryBrush}"
                    Text="StickyDesk" />
                <TextBlock
                    FontSize="15"
                    Foreground="{ThemeResource AppTextSecondaryBrush}"
                    Text="What would you like to open?" />
            </StackPanel>

            <ItemsRepeater Grid.Row="1" ItemsSource="{x:Bind Cards}">
                <ItemsRepeater.Layout>
                    <UniformGridLayout
                        ItemsStretch="Fill"
                        MinColumnSpacing="16"
                        MinItemHeight="140"
                        MinItemWidth="220"
                        MinRowSpacing="16" />
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="local:HomeCard">
                        <Button
                            HorizontalAlignment="Stretch"
                            AutomationProperties.AutomationId="{x:Bind Target}"
                            AutomationProperties.Name="{x:Bind Title}"
                            Click="Card_Click"
                            Style="{StaticResource CardButton}"
                            Tag="{x:Bind Target}">
                            <StackPanel Spacing="12">
                                <Border
                                    Width="44"
                                    Height="44"
                                    HorizontalAlignment="Left"
                                    Background="{x:Bind AccentBrush}"
                                    CornerRadius="11">
                                    <FontIcon
                                        FontSize="22"
                                        Foreground="White"
                                        Glyph="{x:Bind Glyph}" />
                                </Border>
                                <StackPanel Spacing="2">
                                    <TextBlock
                                        FontSize="16"
                                        FontWeight="SemiBold"
                                        Foreground="{ThemeResource AppTextPrimaryBrush}"
                                        Text="{x:Bind Title}" />
                                    <TextBlock
                                        FontSize="12"
                                        Foreground="{ThemeResource AppTextSecondaryBrush}"
                                        Text="{x:Bind Subtitle}"
                                        TextWrapping="Wrap" />
                                </StackPanel>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </Grid>
    </ScrollViewer>
</Page>
